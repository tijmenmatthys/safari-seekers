using System.Collections;
using System.Collections.Generic;
using System.Runtime.CompilerServices;
using UnityEngine;
using UnityEngine.Events;

public class MissionReviewScreen : MonoBehaviour
{

    [SerializeField]
    private float _timeUntilCheck;

    [SerializeField]
    private float _timeBetweenChecks;

    [SerializeField]
    private GameObject _criteriaOne;
    [SerializeField]
    private GameObject _criteriaTwo;
    [SerializeField]
    private GameObject _criteriaThree;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
